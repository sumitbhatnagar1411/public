server:
  port: 8083

spring:
  application:
    name: fintech-caching-reference

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
